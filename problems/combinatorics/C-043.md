---
id: C-043
title: "Binary Families Without Shattered Coordinates"
category: combinatorics
contest: "37th Iranian Mathematical Olympiad"
year: null
source_ref: MathNet 07f7
difficulty: 3
difficulty_note: "需按首位将族压缩成只出现一次与成对出现的后缀族，建立 Pascal 型递推并配以低汉明重量构造。"
topics: ["递推与生成结构"]
verification: mathnet-reviewed
mathnet_id: "07f7"
review_ref: data/review/comb-08/verdicts.json
source_url: https://huggingface.co/datasets/ShadenA/MathNet
---

# C-043｜Binary Families Without Shattered Coordinates

## 题面

Let $n, k$ be positive integers such that $n \ge k$. Find the maximum number of binary sequences of length $n$ such that by fixing any arbitrary $k$ bits, the achieved sequences do not produce all binary sequences of length $k$. For example if $k = 1$, we can only have one sequence, otherwise they will differ in at least one bit which means that fixing that bit produces all binary sequences of length 1.

## 答案

sum_{i=0}^{k-1} binom(n,i)

## 解法要点

Let the answer be $f(n,k)$. We will prove by induction on $n+k$ that
$$
f(n,k) = \sum_{i=0}^{k-1} \binom{n}{i}.
$$
Case $n=k=1$ is obvious.

Assume that $S$ is the maximum set of the desired sequences and let $T$ be the set of all binary sequences of length $n-1$. Define the sets $A, B, C$ as follows:
$$
\begin{align*}
A &= \{a \in T \mid \text{there exists } b \in S \text{ with } b = \overline{0a}, \text{ but there is no } c \in S \text{ with } c = \overline{1a}\} \\
B &= \{a \in T \mid \text{there exists } b \in S \text{ with } b = \overline{1a}, \text{ but there is no } c \in S \text{ with } c = \overline{0a}\} \\
C &= \{a \in T \mid \text{there exists } b, c \in S \text{ with } b = \overline{0a}, c = \overline{1a}\}.
\end{align*}
$$
Clearly, $f(n,k) = |A| + |B| + 2|C|$. Because we can't choose $k$ bits from the last $n-1$ bits of the sequences that produce all the binary sequences of length $k$, we have $|A| + |B| + |C| \le f(n-1,k)$. Now consider the set $C$. If $|C| \ge f(n-1,k-1)$, we can fix $k-1$ bits from the last $n-1$ bits to produce all sequences of length $k-1$. So because of the definition of $C$, by fixing these $k-1$ bits and the first bit, all sequences of length $k$ are produced, which is a contradiction. So we have
$$
f(n,k) = |A| + |B| + 2|C| \le f(n-1,k) + f(n-1,k-1).
$$
By induction and using Pascal's identity we get
$$
\begin{align*}
f(n,k) &\le \sum_{i=0}^{k-1} \binom{n-1}{i} + \sum_{i=0}^{k-2} \binom{n-1}{i} \\
&= 1 + \sum_{i=1}^{k-1} \binom{n-1}{i} + \sum_{i=0}^{k-2} \binom{n-1}{i} \\
&= 1 + \sum_{i=1}^{k-1} \left( \binom{n-1}{i} + \binom{n-1}{i-1} \right) \\
&= \binom{n}{0} + \sum_{i=1}^{k-1} \binom{n}{i} = \sum_{i=0}^{k-1} \binom{n}{i}.
\end{align*}
$$
For the equality example, simply choose all binary sequences with at most $k-1$ zeros. This way, by fixing any $k$ bits, the sequence with $k$ zeros will never be produced and we're done. ■

## 提示阶梯

1. 按第一位是 0 还是 1 来拆分序列族，并特别关注删去第一位后同时能以两种首位出现的尾串。这个拆分应导出关于长度的递推。
2. 将尾串分成只配首位 0、只配首位 1、以及两种首位都出现的三类。三类尾串的并不能在任意 k 个坐标上产生全部模式，而“双首位”那一类还不能在任意 k-1 个尾坐标上产生全部模式。
3. 由三类的计数写出 f(n,k)≤f(n-1,k)+f(n-1,k-1)，再用归纳与 Pascal 恒等式化简。取所有零的个数至多为 k-1 的序列作匹配构造，因为固定任意 k 位时全零模式不会出现。
