---
id: N-003
title: "Triangle-Factor Equation Modulo Three"
category: number-theory
contest: "Baltic Way 2016"
year: 2016
source_ref: MathNet 01dn
difficulty: 2
difficulty_note: "按 a,b,c 模三是否含零及正负一分情形比较等式两边，即可迫出三者公因子三。（规则估★3/Codex★2，就低取★2）"
topics: ["最大公约数与欧几里得算法", "素数与算术基本定理", "整除与同余"]
verification: mathnet-reviewed
mathnet_id: "01dn"
review_ref: data/review/import-01/verdicts.json
source_url: https://huggingface.co/datasets/ShadenA/MathNet
---

# N-003｜Triangle-Factor Equation Modulo Three

## 题面

Do there exist positive integers $a, b, c$, such they have no common divisor and
$$
ab + bc + ca = (a + b - c)(b + c - a)(c + a - b)?
$$

## 答案

No; any solution would force all three integers to be divisible by three, so none exist without a common divisor.

## 解法要点

We show that all of $a, b$ and $c$ have the number $3$ as a common factor. First suppose that one of $a, b, c$ is divisible by $3$. By symmetry, we may assume that $a \equiv 0 \pmod{3}$. Then the equation implies $bc \equiv (b-c)(b+c)(c-b) \pmod{3}$. If neither of $b$ and $c$ is divisible by $3$, this gives $bc \equiv 0 \pmod{3}$, which is a contradiction. On the other hand, if either of $b$ and $c$ is divisible by $3$, then from $bc \equiv (b-c)(b+c)(c-b) \pmod{3}$ we see that they both are divisible by $3$, which means that $a, b$ and $c$ are all divisible by $3$.

Now we are left with the case that $a, b, c$ are each $\pm 1 \pmod{3}$. If $a \equiv b \equiv c \pmod{3}$, then clearly the left-hand side of the equation is divisible by $3$, while the right-hand side is not, so we have a contradiction. In the opposite case that two of $a, b$ and $c$ are equal and the third one is distinct modulo $3$, clearly the left-hand side of the equation is not divisible by $3$, while the right-hand side is divisible. We conclude that $a, b$ and $c$ are all divisible by $3$.

## 提示阶梯

1. 只看原等式模 $3$，按三个数中是否已有一个被 $3$ 整除来分类。
2. 若某一个数模 $3$ 为零，利用对称性固定它，再比较剩余两数在右边三个因子中的出现方式；若都非零，则每个数只能为 $±1$。
3. 在都为 $±1$ 的分支，只需分成三者同余与恰有两者同余两类，逐类比较等式两边是否被 $3$ 整除；再与“无公因数”条件对照。
