---
id: N-024
title: "Base-b Numerals Divisible by Sixteen"
category: number-theory
contest: "AMC 10 A"
year: null
source_ref: MathNet 0kzy
difficulty: 2
difficulty_note: "关键是把进制数因式分解后按模 8 分类，再对三个有效剩余类作区间计数，属于短链常规组合。"
topics: ["素数与算术基本定理", "整除与同余", "多项式"]
verification: mathnet-reviewed
mathnet_id: "0kzy"
review_ref: data/review/nt-04/verdicts.json
source_url: https://huggingface.co/datasets/ShadenA/MathNet
---

# N-024｜Base-b Numerals Divisible by Sixteen

## 题面

There are exactly $K$ positive integers $b$ with $5 \le b \le 2024$ such that the base-$b$ integer $2024_b$ is divisible by 16 (where 16 is in base ten). What is the sum of the digits of $K$?
(A) 16 (B) 17 (C) 18 (D) 20 (E) 21

## 答案

D

## 解法要点

Notice that $2024_b = 2b^3 + 2b + 4 = 2(b+1)(b^2 - b + 2)$, and consider the residue classes of this number modulo 8. If $b \equiv 7 \pmod{8}$, then $b+1 \equiv 0 \pmod{8}$, and if $b \equiv 3 \pmod{8}$, then $b^2 - b + 2 \equiv 0 \pmod{8}$. In each case $2024_b$ is divisible by 16.
In all other cases $2024_b$ is not divisible by 16. Indeed, if $b \equiv 0, 2, \text{ or } 4 \pmod{8}$, then $b+1$ is odd, and $b^2 - b + 2 \equiv b + 2 \pmod{8}$, so $2024_b$ is divisible by no power of 2 greater than $2^3$. If $b \equiv 1 \pmod{8}$, then $b+1$ and $b^2 - b + 2$ are both odd multiples of 2, so $2024_b$ is divisible by 8, but not by 16.
Because $2024 = 253 \cdot 8$, there are $253 \cdot 3 = 759$ positive integers $b \le 2024$ that are congruent to 3, 6, or 7 modulo 8. The number 3 must be excluded from this total, because the problem statement requires $b$ to be at least 5. Thus $K = 759 - 1 = 758$, and the sum of the digits of $K$ is $7+5+8 = 20$.

## 提示阶梯

1. 先把这个 base-b 数写成关于 b 的多项式，并把整除条件转化为考察其中的 2 因子。
2. 将多项式因式分解为 2(b+1)(b^2-b+2)，再按 b 模 8 的余数分类判断两个因子的偶性。
3. 逐一检查模 8 的各类：有的类使 b+1 含足够多的 2 因子，有的类使 b^2-b+2 含足够多的 2 因子；确认其余类不足后，再在给定区间内计数。
