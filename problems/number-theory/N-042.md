---
id: N-042
title: "Perfect-Power Avoidance in Arithmetic Progressions"
category: number-theory
contest: "Bulgarian National Olympiad - Final Round"
year: null
source_ref: MathNet 03ft
difficulty: 3
difficulty_note: "需选取两个辅助素数并精确设计等差数列公差，使两个素数赋值恒为相邻整数，从而排除所有完全幂，构造突破口不常见。（规则估★3/Codex★4，就低取★3）"
topics: ["阶与原根", "素数与算术基本定理", "p-adic 赋值与 LTE"]
verification: mathnet-reviewed
mathnet_id: "03ft"
review_ref: data/review/nt-08/verdicts.json
source_url: https://huggingface.co/datasets/ShadenA/MathNet
---

# N-042｜Perfect-Power Avoidance in Arithmetic Progressions

## 题面

Is it true that for any positive integer $n > 1$, there exists an infinite arithmetic progression $M_n$ of positive integers, such that for any $m \in M_n$, the number $n^m - 1$ is not a perfect power (a positive integer is a perfect power if it is of the form $a^b$ for positive integers $a, b > 1$)?

## 答案

Yes

## 解法要点

(Victor Kostadinov) The answer is yes. Fix a positive integer $n$ and two large distinct primes $p, q > n$. Let $d_p = \text{ord}_p(n)$, $d_q = \text{ord}_q(n)$, $c_p = \nu_p(n^{d_p} - 1)$, $c_q = \nu_q(n^{d_q} - 1)$ and let $c = \nu_p(d_q)$, $d = \nu_q(d_p)$. Pick two sufficiently large constants $a, b$, and let $M := d_p d_q p^{(a-1)c_p + bc_q - c} q^{ac_p + (b-1)c_q + 1 - d}$ and finally choose $M_n$ to consist of $m = M(1 + iM)$ for $i = 1, 2, \dots$.

By LTE, we have
$$
\nu_p(n^m - 1) = \nu_p(n^{d_p} - 1) + \nu_p\left(\frac{m}{d_p}\right) = c_p + c + (a-1)c_p + bc_q - c = ac_p + bc_q
$$
(we used that $\text{gcd}(1 + iM, p) = 1$) and similarly $\nu_q(n^m - 1) = ac_p + bc_q + 1$, which are consecutive, i.e. they can't have a common divisor greater than 1 and thus $n^m - 1$ can't be a perfect power. $\square$

## 提示阶梯

1. 要排除完美幂，可设法让 $n^m-1$ 的两个素数赋值互素；因此先选两个大于 $n$ 的不同素数，并控制指数 $m$。
2. 记 $d_p=ord_p(n)$、$d_q=ord_q(n)$，把 $m$ 取为同时含 $d_p,d_q$ 及适当 $p,q$ 次幂的倍数；LTE 会把两个赋值化成关于这些指数的线性式。
3. 先构造 $M$ 使 $v_p(n^M-1)$ 与 $v_q(n^M-1)$ 相邻，再令 $m=M(1+iM)$。因 $1+iM$ 不增添这两个素数的赋值，整个等差数列上两赋值始终相邻。
