---
id: N-042
title: "Divisibility of an Exponential Quotient"
category: number-theory
contest: "IMO Selection"
year: null
source_ref: MathNet 0g42
difficulty: 4
difficulty_note: "需以 n 的最小素因子结合乘法阶迫使 11∣n，再调用 LTE 精确控制商的 11-adic 赋值，形成多引理串联。"
topics: ["整除与同余", "最大公约数与欧几里得算法", "费马小定理与欧拉定理", "阶与原根"]
verification: mathnet-reviewed
mathnet_id: "0g42"
review_ref: data/review/nt-07/verdicts.json
source_url: https://huggingface.co/datasets/ShadenA/MathNet
---

# N-042｜Divisibility of an Exponential Quotient

## 题面

Problem:
Let $n \geq 2$ be an integer. Prove that if
$$
\frac{n^{2}+4^{n}+7^{n}}{n}
$$
is an integer, then it is divisible by $11$.

## 答案

证明题

## 解法要点

Solution:
The "Lifting the Exponent" lemma tells us that
$$
v_{11}\left(4^{n}+7^{n}\right)=v_{11}(4+7)+v_{11}(n)=1+v_{11}(n)
$$
and we therefore have $11 \mid\left(4^{n}+7^{n}\right) / n$.

Now let $p$ denote the smallest prime factor of $n$. By assumption we have $p \mid n \mid 4^{n}+7^{n}$, with $p \neq 2,7$. Now setting $x \equiv 4 \cdot 7^{-1} \bmod p$, we find:
$$
4^{n} \equiv -7^{n} \bmod p \Longrightarrow x^{n} \equiv -1 \quad \bmod p \Longrightarrow x^{2 n} \equiv 1 \quad \bmod p
$$
If $d$ denotes the order of $x \bmod p$, the above implies $d \mid 2 n$. By Euler-Fermat we also have $d \mid \varphi(p)=p-1$. By minimality of $p$, we have $\gcd(n, p-1)=1$, and thus $d \mid 2$. Therefore,
$$
x^{2} \equiv 1 \quad \bmod p \Longrightarrow 4^{2} \equiv 7^{2} \quad \bmod p \Longrightarrow p \mid 49-16=33 \Longrightarrow p=3,11
$$
But since $4^{n} \equiv 7^{n} \equiv 1 \bmod 3$, we cannot have $p=3 \mid n$, since otherwise $n$ does not divide $4^{n}+7^{n}$. We conclude that $p=11 \mid n$ and the result follows.

## 提示阶梯

1. 先考察 n 的最小素因子，把整除条件转成 4^n+7^n 在该素数模下的关系。
2. 设 p 为 n 的最小素因子，令 x≡4·7^(-1) (mod p)；x^n≡-1 使 x 的阶同时整除 2n 与 p-1，而最小性给出 gcd(n,p-1)=1。
3. 因此 x 的阶至多为 2，从 x²≡1 推出 p 整除 7²-4²；排除其中与原整除条件冲突的候选后，再结合 v_11(4^n+7^n)=1+v_11(n) 处理原商。
