---
id: N-045
title: "Distinct Decimal Orders"
category: number-theory
contest: "International Mathematical Olympiad"
year: null
source_ref: MathNet 06um
difficulty: 4
difficulty_note: "上界虽是直接计数，但达到 807 需构造统一的 m 并用各素数的乘法阶与精确赋值区分所有 c，属于不常见的多引理构造。"
topics: ["阶与原根", "费马小定理与欧拉定理", "素数与算术基本定理", "p-adic 赋值与 LTE"]
verification: mathnet-reviewed
mathnet_id: "06um"
review_ref: data/review/nt-08/verdicts.json
source_url: https://huggingface.co/datasets/ShadenA/MathNet
---

# N-045｜Distinct Decimal Orders

## 题面

Call a rational number short if it has finitely many digits in its decimal expansion. For a positive integer $m$, we say that a positive integer $t$ is $m$-tastic if there exists a number $c \in \{1,2,3, \ldots, 2017\}$ such that $\frac{10^{t}-1}{c \cdot m}$ is short, and such that $\frac{10^{k}-1}{c \cdot m}$ is not short for any $1 \leqslant k<t$. Let $S(m)$ be the set of $m$-tastic numbers. Consider $S(m)$ for $m=1,2, \ldots$. What is the maximum number of elements in $S(m)$ ?

## 答案

807

## 解法要点

If $m=2^{a} \cdot 5^{b} \cdot s$, with $\operatorname{gcd}(s, 10)=1$, then $\frac{10^{t}-1}{m}$ is short if and only if $s$ divides $10^{t}-1$. So we may (and will) suppose without loss of generality that $\operatorname{gcd}(m, 10)=1$. Define
$$
C=\{1 \leqslant c \leqslant 2017: \operatorname{gcd}(c, 10)=1\} .
$$
The $m$-tastic numbers are then precisely the smallest exponents $t>0$ such that $10^{t} \equiv 1 \pmod{c m}$ for some integer $c \in C$, that is, the set of orders of $10$ modulo $c m$. In other words,
$$
S(m)=\left\{\operatorname{ord}_{c m}(10): c \in C\right\} .
$$
Since there are $4 \cdot 201+3=807$ numbers $c$ with $1 \leqslant c \leqslant 2017$ and $\operatorname{gcd}(c, 10)=1$, namely those such that $c \equiv 1,3,7,9 \pmod{10}$,
$$
|S(m)| \leqslant |C|=807 .
$$
Now we find $m$ such that $|S(m)|=807$. Let
$$
P=\{1<p \leqslant 2017: p \text{ is prime, } p \neq 2,5\}
$$
and choose a positive integer $\alpha$ such that every $p \in P$ divides $10^{\alpha}-1$ (e.g. $\alpha=\varphi(T)$, $T$ being the product of all primes in $P$), and let $m=10^{\alpha}-1$.

Claim. For every $c \in C$, we have
$$
\operatorname{ord}_{c m}(10)=c \alpha
$$
As an immediate consequence, this implies $|S(m)|=|C|=807$, finishing the problem.

Proof. Obviously $\operatorname{ord}_{m}(10)=\alpha$. Let $t=\operatorname{ord}_{c m}(10)$. Then
$$
c m \mid 10^{t}-1 \quad \Longrightarrow \quad m \mid 10^{t}-1 \quad \Longrightarrow \quad \alpha \mid t .
$$
Hence $t=k \alpha$ for some $k \in \mathbb{Z}_{>0}$. We will show that $k=c$.

Denote by $\nu_{p}(n)$ the number of prime factors $p$ in $n$, that is, the maximum exponent $\beta$ for which $p^{\beta} \mid n$. For every $\ell \geqslant 1$ and $p \in P$, the Lifting the Exponent Lemma provides
$$
\nu_{p}\left(10^{\ell \alpha}-1\right)=\nu_{p}\left(\left(10^{\alpha}\right)^{\ell}-1\right)=\nu_{p}\left(10^{\alpha}-1\right)+\nu_{p}(\ell)=\nu_{p}(m)+\nu_{p}(\ell)
$$
So
$$
\begin{aligned}
c m \mid 10^{k \alpha}-1 & \Longleftrightarrow \forall p \in P ; \quad \nu_{p}(c m) \leqslant \nu_{p}\left(10^{k \alpha}-1\right) \\
& \Longleftrightarrow \forall p \in P ; \quad \nu_{p}(m)+\nu_{p}(c) \leqslant \nu_{p}(m)+\nu_{p}(k) \\
& \Longleftrightarrow \forall p \in P ; \quad \nu_{p}(c) \leqslant \nu_{p}(k) \\
& \Longleftrightarrow c \mid k .
\end{aligned}
$$
The first such $k$ is $k=c$, so $\operatorname{ord}_{c m}(10)=c \alpha$.

## 提示阶梯

1. 先去掉 $m$ 中的 $2,5$ 因子，把“有限小数”条件改写成模 $cm$ 的乘法阶问题。
2. 于是 $S(m)$ 是所有 $ord_{cm}(10)$ 组成的集合，其中 $1≤c≤2017$ 且 $(c,10)=1$；其大小先由可选的 $c$ 数量给出上界，关键是构造一个 $m$ 让这些阶互不相同。
3. 取 $α$ 使所有相关素数都整除 $10^α-1$，并令 $m=10^α-1$。写 $ord_{cm}(10)=kα$，再用 LTE 逐个比较素因子赋值，可把 $cm∣10^{kα}-1$ 等价为 $c∣k$。
