---
id: A-020
title: "Nested Radical Integrality"
category: algebra
contest: "HMMT Spring 2021 Team Round"
year: 2021
source_ref: MathNet 0kgc
difficulty: 2
difficulty_note: "平方并利用整数平方根的有理性判别可解决第一问，再构造一个简短反例解决第二问。（规则估★3/Codex★2，就低取★2）"
topics: ["代数变换技巧"]
verification: mathnet-reviewed
mathnet_id: "0kgc"
review_ref: data/review/alg-02/verdicts.json
source_url: https://huggingface.co/datasets/ShadenA/MathNet
---

# A-020｜Nested Radical Integrality

## 题面

Problem:

Let $a$ and $b$ be positive integers with $a > b$. Suppose that
$$
\sqrt{\sqrt{a} + \sqrt{b}} + \sqrt{\sqrt{a} - \sqrt{b}}
$$
is an integer.

a. Must $\sqrt{a}$ be an integer?

b. Must $\sqrt{b}$ be an integer?

## 答案

a: Yes. b: No; for example, a = 36 and b = 32.

## 解法要点

Solution:

Let $r = \sqrt{\sqrt{a} + \sqrt{b}}$ and $s = \sqrt{\sqrt{a} - \sqrt{b}}$. We know $r^{2} + s^{2} = 2 \sqrt{a}$ and $r^{2} - s^{2} = 2 \sqrt{b}$. If $r + s$ is an integer $k$, then
$$
\sqrt{a} = \frac{r^{2} + s^{2}}{2} = \frac{(r + s)^{2} + (r - s)^{2}}{4} = \frac{k^{2} + 4b / k^{2}}{4},
$$
which is rational. Recall that since $a$ is a positive integer, $\sqrt{a}$ is either an integer or an irrational number. (Proof: if $\sqrt{a} = p / q$ for relatively prime positive integers $p, q$, then $p^{2} / q^{2}$ is an integer, which implies $q = 1$.) Thus the answer to part (a) is yes.

The answer to part (b) is no because
$$
(2 \pm \sqrt{2})^{2} = 6 \pm \sqrt{32}
$$
meaning that setting $a = 36$ and $b = 32$ is a counterexample.

---

Solution:

Second solution for part (a): Squaring $\sqrt{\sqrt{a} + \sqrt{b}} + \sqrt{\sqrt{a} - \sqrt{b}}$, we see that $2 \sqrt{a} + 2 \sqrt{a - b}$ is an integer. Hence $\sqrt{a - b} = m - \sqrt{a}$ for some rational number $m$. Squaring both sides of this, we see that $a - b = m^{2} - 2m \sqrt{a} + a$, so $\sqrt{a} = \frac{m^{2} + b}{2m}$, a rational number. As in the first solution, it follows that $\sqrt{a}$ is an integer.

## 提示阶梯

1. 关注两个外层根式的和、差及平方后与 $\sqrt a,\sqrt b$ 的关系；两小问需要分别考察有理性与共轭式。
2. 若设两根式为 $r,s$ 且 $r+s=k$，则 $r^2+s^2=2\sqrt a$，而 $rs=\sqrt{a-b}$；还可由 $(r-s)^2$ 把 $\sqrt a$ 表成含整数 $k$ 与 $b$ 的有理式。
3. 推出 $\sqrt a=(k^2+4b/k^2)/4$ 后，使用“整数的平方根若有理则为整数”。另一问可展开 $(2\pm\sqrt2)^2$，把所得一对共轭式与两个内层被开方数对照，检验它们是否满足题设。
