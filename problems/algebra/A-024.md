---
id: A-024
title: "Quadratic Functional Equation"
category: algebra
contest: "IMO Selektion"
year: null
source_ref: MathNet 0fxw
difficulty: 3
difficulty_note: "非零情形先由值域含半轴推出偶性，再比较正负输入强制得到二次函数形式，包含一个非显然的值域突破口。（规则估★4/Codex★3，就低取★3）"
topics: ["函数方程"]
verification: mathnet-reviewed
mathnet_id: "0fxw"
review_ref: data/review/alg-02/verdicts.json
source_url: https://huggingface.co/datasets/ShadenA/MathNet
---

# A-024｜Quadratic Functional Equation

## 题面

Problem:
Finde alle Funktionen $f: \mathbb{R} \rightarrow \mathbb{R}$, sodass für alle reellen $x, y$ gilt
$$
f(f(x)-f(y))=(x-y)^2 f(x+y)
$$

## 答案

f(x) = 0, f(x) = x^2, f(x) = -x^2

## 解法要点

Solution:
Mit $x=y$ folgt $f(0)=0$. Mit $x=0$ respektive $y=0$ erhält man die beiden Gleichungen $f(-f(y))=y^2 f(y)$ und $f(f(x))=x^2 f(x)$ und durch Kombination weiter
$$
f(f(x))=f(-f(x)) \quad \forall x \in \mathbb{R}
$$
Offenbar ist die Nulfunktion $f \equiv 0$ eine Lösung, wir nehmen daher im Folgenden an, dass $f(a) \neq 0$ gilt für ein $a \in \mathbb{R}$. Mit $y=a-x$ erhält man dann
$$
f(f(x)-f(a-x))=(2x-a)^2 f(a)
$$
und hier nimmt die rechte Seite alle positiven respektive alle negativen reellen Werte an, je nach Vorzeichen von $f(a)$. Dasselbe gilt für die linke Seite und somit auch für $f$. Zusammen mit (4) zeigt das in beiden Fällen, dass $f$ eine gerade Funktion ist.

Ersetzt man nun in der ursprünglichen Gleichung $y$ durch $-y$, dann ändert sich die linke Seite nicht, und ein Vergleich der alten und neuen rechten Seite ergibt die Gleichung
$$
(x-y)^2 f(x+y)=(x+y)^2 f(x-y)
$$
Setzt man hier schliesslich noch $y=x-1$, dann folgt $f(2x-1)=(2x-1)^2 f(1)$, also sind alle Lösungen von der Gestalt $f(x)=c x^2$ für eine Konstante $c \neq 0$. Einsetzen zeigt $c= \pm 1$ und die drei Lösungen sind
$$
f(x)=0, \quad f(x)=x^2, \quad f(x)=-x^2
$$

## 提示阶梯

1. 先关注原点处的函数值、复合值 $f(\pm f(x))$ 与函数奇偶性；零函数情形应单独保留。
2. 取 $x=y$、$x=0$、$y=0$ 可得基础关系。固定一个 $f(a)
e0$，令 $y=a-x$，由函数值覆盖同一符号的半轴并结合 $f(f(x))=f(-f(x))$ 推出偶性。
3. 把原式中的 $y$ 换成 $-y$ 并比较，得到 $(x-y)^2f(x+y)=(x+y)^2f(x-y)$；再令 $y=x-1$ 迫使 $f(t)=ct^2$，最后代回原式筛选常数，并别忘了先前分出的零函数。
